spring.application.name=BikeSelling

# Server Configuration
server.port=8080

# Supabase PostgreSQL Configuration
# Get credentials from environment variables
# Set before running: $env:SUPABASE_DB_URL="jdbc:postgresql://..."
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.puquylitonftpyyusgqj
spring.datasource.password=Xedapcu@1234
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration for PostgreSQL
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# JWT Configuration
jwt.secret=YourSuperSecretKeyForJWTTokenGenerationMustBeLongEnoughForHS512AlgorithmToWorkProperly1234567890
jwt.expiration=86400000

# Google OAuth2 Configuration
# Get credentials from environment variables
# Set before running: $env:GOOGLE_CLIENT_ID="..." $env:GOOGLE_CLIENT_SECRET="..."
spring.security.oauth2.client.registration.google.client-id=${GOOGLE_CLIENT_ID:your-client-id}
spring.security.oauth2.client.registration.google.client-secret=${GOOGLE_CLIENT_SECRET:your-client-secret}
spring.security.oauth2.client.registration.google.scope=profile,email
spring.security.oauth2.client.registration.google.redirect-uri={baseUrl}/login/oauth2/code/{registrationId}

# Logging
logging.level.swp.be.vn.bs=DEBUG
logging.level.org.springframework.security=DEBUG

# SpringDoc OpenAPI (Swagger) Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true
springdoc.swagger-ui.operationsSorter=method
springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.tryItOutEnabled=true
springdoc.packages-to-scan=swp.be.vn.bs.controller
springdoc.show-actuator=false

# OAuth2 Frontend Redirect Configuration
oauth2.frontend.url=${FRONTEND_URL:http://localhost:5173}
oauth2.frontend.redirect-path=/oauth2/redirect
